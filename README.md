# agenda

To run the viewer,

```
cd viewer
yarn
yarn start
```

To run the crawler,

```
cd crawler
yarn
node index
```

Results are in `crawler/data/index.json`.

To modify the sources to crawl, edit `crawler/config.js`.
